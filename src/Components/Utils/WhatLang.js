export const WhatLanguage = localStorage.getItem('forlanso_language');

/* let langText;
if(lang === 'en' || lang === undefined){
    langText=  'en' 
}else if(lang === 'ar'){
    langText= 'ar'
}

export default langText; */
